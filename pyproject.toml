[project]
name = "mediastorm-club-checkin"
version = "0.1.0"
dependencies = ["requests==2.32.3"]

[project.optional-dependencies]
dev = ["pytest==8.3.5"]

[tool.setuptools]
py-modules = ["checkin"]

[tool.pytest.ini_options]
pythonpath = ["."]
